---
phase: 02-chat-core
plan: 05
type: execute
wave: 3
depends_on:
  - 02-03
  - 02-04
files_modified:
  - components/chat/ChatInput.tsx
  - components/chat/MessageBubble.tsx
  - components/chat/MessageList.tsx
  - hooks/useAmplifyChat.ts
  - app/api/chat/route.ts
  - tests/chat/api.test.ts
  - tests/chat/orchestrator.test.ts
  - tests/chat/persistence.test.ts
  - tests/campaigns/queries.test.ts
autonomous: false
requirements:
  - INFRA-07
  - CHAT-04
  - CHAT-05
  - CHAT-10

must_haves:
  truths:
    - "Error states show user-friendly messages with retry option — never a blank screen"
    - "Loading skeleton appears immediately when user sends a message, before tokens arrive"
    - "Stop mid-stream preserves partial content and shows retry chip"
    - "Chat is fully usable at 375px — no horizontal scroll, readable text, tappable inputs"
    - "All todo test stubs from Plan 00 are implemented and pass"
  artifacts:
    - path: "tests/chat/api.test.ts"
      provides: "Implemented tests for auth guard and streaming response"
    - path: "tests/chat/orchestrator.test.ts"
      provides: "Implemented tests for model selection and tone injection"
    - path: "tests/chat/persistence.test.ts"
      provides: "Implemented tests for message persistence"
    - path: "tests/campaigns/queries.test.ts"
      provides: "Implemented tests for campaign queries"
  key_links:
    - from: "hooks/useAmplifyChat.ts"
      to: "components/chat/MessageList.tsx"
      via: "error state propagation"
      pattern: "error|isError"
    - from: "components/chat/ChatInput.tsx"
      to: "hooks/useAmplifyChat.ts"
      via: "stop() and reload() calls"
      pattern: "stop|reload"
---

<objective>
Add error handling and loading states throughout the chat, implement all remaining test stubs, and perform a mobile responsiveness verification checkpoint. This is the polish and quality gate plan.

Purpose: INFRA-07 requires error handling across all AI operations. This plan also closes all test gaps and verifies CHAT-10 (375px mobile usability).
Output: Production-quality error UX, complete test coverage, verified mobile experience.
</objective>

<execution_context>
@/Users/abhishekratna/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abhishekratna/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-chat-core/02-CONTEXT.md
@.planning/phases/02-chat-core/02-RESEARCH.md
@.planning/phases/02-chat-core/02-03-SUMMARY.md
@.planning/phases/02-chat-core/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error handling, loading states, and test implementation</name>
  <files>hooks/useAmplifyChat.ts, components/chat/MessageList.tsx, components/chat/MessageBubble.tsx, components/chat/ChatInput.tsx, app/api/chat/route.ts, tests/chat/api.test.ts, tests/chat/orchestrator.test.ts, tests/chat/persistence.test.ts, tests/campaigns/queries.test.ts</files>
  <read_first>
    - hooks/useAmplifyChat.ts (current implementation — add onError handler)
    - components/chat/MessageList.tsx (add loading skeleton at bottom during streaming)
    - components/chat/ChatInput.tsx (disable during error state, show retry)
    - app/api/chat/route.ts (current implementation — verify campaign check is before message persistence, add try/catch around orchestrator call)
    - tests/chat/api.test.ts (existing todo stubs to implement)
    - tests/chat/orchestrator.test.ts (existing todo stubs to implement)
    - tests/chat/persistence.test.ts (existing todo stubs to implement)
    - tests/campaigns/queries.test.ts (existing todo stubs to implement)
    - tests/helpers/supabase.ts (mock helpers)
    - lib/ai/orchestrator.ts (for orchestrator test mocking)
    - lib/db/campaigns.ts (for campaign query testing)
    - design-system/amplify-aol/MASTER.md (error color: #EF4444, loading skeleton spec)
  </read_first>
  <action>
1. **Error handling in useAmplifyChat:**
   - Add `onError` callback to useChat options:
     ```typescript
     onError: (error) => {
       console.error('Chat error:', error)
       // The error state is automatically managed by useChat
     }
     ```
   - useChat already exposes `error` and `isLoading` — pass these through the hook return

2. **Error display in MessageList:**
   - When `error` is truthy, render an error banner at the bottom of the message list:
     - `bg-red-50 border border-red-200 rounded-xl p-4 mx-4`
     - `text-red-700 text-sm` with the error message
     - A "Try Again" button: `bg-white border border-red-300 text-red-600 rounded-full px-4 py-1.5 text-sm hover:bg-red-50 cursor-pointer transition-colors duration-200`
     - "Try Again" calls `reload()` from useAmplifyChat

3. **Loading skeleton in MessageList:**
   - When `isLoading` is true and no streaming content has appeared yet (last message is user's), show a `<SkeletonPart type="text" />` in an AI bubble at the bottom
   - This appears immediately when user sends, before any tokens arrive
   - The skeleton disappears once the first text delta arrives (useChat handles this automatically by adding the assistant message)

4. **ChatInput error state:**
   - When `error` is truthy, show a small inline error indicator: `text-red-500 text-xs` below the textarea
   - Disable the send button during error until user types a new message (the input itself stays enabled)

5. **API route error handling:**
   - Verify that `app/api/chat/route.ts` already has the campaign existence check BEFORE message persistence (this was fixed in Plan 02-01). If not, ensure the order is:
     1. Auth guard (401)
     2. Field validation (400)
     3. Campaign existence check (404) — `campaigns.select('id').eq('id', campaignId).single()` — MUST be before any `campaign_messages.insert`
     4. User message persistence
     5. runStreamingTask call
   - Wrap the `runStreamingTask` call in a try/catch:
     ```typescript
     try {
       const result = await runStreamingTask('chat.orchestrate', { ... })
       return result.toDataStreamResponse()
     } catch (error) {
       console.error('Chat API error:', error)
       return new Response(
         JSON.stringify({ error: 'Failed to generate response. Please try again.' }),
         { status: 500, headers: { 'Content-Type': 'application/json' } }
       )
     }
     ```

6. **Implement all test stubs:**

   **tests/chat/api.test.ts:**
   - Mock `@/lib/supabase/server` to return a mock client
   - Test: unauthenticated request returns 401
   - Test: missing messages returns 400
   - Test: missing campaignId returns 400
   - Test: valid request calls runStreamingTask and returns streaming response

   **tests/chat/orchestrator.test.ts:**
   - Mock `@/lib/ai/models` and `@/lib/prompts/registry`
   - Test: `runStreamingTask('chat.orchestrate', ...)` calls `streamText` with the correct model
   - Test: tone variable is injected into rendered system prompt
   - Test: sliding window limits messages to last 20 items

   **tests/chat/persistence.test.ts:**
   - Mock Supabase client
   - Test: after stream finishes, `campaign_messages.insert` is called with role='assistant' and parts array
   - Test: user message is persisted before streaming starts

   **tests/campaigns/queries.test.ts:**
   - Mock Supabase client
   - Test: `getUserCampaigns` calls with `order('updated_at', { ascending: false })`
   - Test: `createCampaign` inserts with correct fields
   - Test: `getCampaignWithMessages` loads campaign and messages in parallel

   All tests should use vitest `vi.mock()` for module mocking. Follow the existing pattern in `tests/helpers/supabase.ts`.
  </action>
  <verify>
    <automated>cd "/Users/abhishekratna/Documents/AOL AI/Amplify-Aol" && pnpm test -- --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/useAmplifyChat.ts` has `onError` callback in useChat options
    - `components/chat/MessageList.tsx` renders error banner when `error` is truthy with "Try Again" button
    - `components/chat/MessageList.tsx` renders `SkeletonPart` while `isLoading` and last message is user's
    - `app/api/chat/route.ts` has try/catch around runStreamingTask call returning 500 on failure
    - `app/api/chat/route.ts` campaign existence check happens BEFORE any `campaign_messages.insert` call
    - `tests/chat/api.test.ts` has no `it.todo` — all tests are implemented
    - `tests/chat/orchestrator.test.ts` has no `it.todo` — all tests are implemented
    - `tests/chat/persistence.test.ts` has no `it.todo` — all tests are implemented
    - `tests/campaigns/queries.test.ts` has no `it.todo` — all tests are implemented
    - `pnpm test -- --run` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>Error handling covers API errors (try/catch + 500), client errors (error banner + retry), and validation errors (400/404 with campaign check before persistence). Loading skeletons appear immediately during streaming. All test stubs are implemented and passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 human verification checkpoint</name>
  <action>
Human verifies the complete Phase 2 chat experience end-to-end. This checkpoint covers streaming, campaign management, rich content rendering, error handling, and mobile responsiveness.

What was built: Complete Phase 2 chat interface including streaming conversation with Claude, campaign management sidebar, rich content part renderers, channel-native previews, action chips, error handling, edit-last-message, mobile responsiveness, and all tests passing.

How to verify:
1. Open http://localhost:3000 (or staging URL) on desktop
2. Verify sidebar shows "New Campaign" button and campaign list
3. Click "New Campaign" — verify campaign type selector appears with 9 types
4. Select "Introductory Workshop" — verify redirect to /chat/{new-id}
5. Type "I want to promote a yoga workshop in Bangalore next month" and send
6. Verify tokens stream in real time in gray AI bubble (left-aligned)
7. Verify your message appears in blue bubble (right-aligned)
8. Verify campaign title auto-generates in sidebar (e.g., "Bangalore Yoga Workshop")
9. Click the stop button during streaming — verify partial content is preserved
10. Click the regenerate button (rotate icon) below AI response — verify new response streams
11. Change tone to "Formal" and send another message — verify AI tone changes
12. Click the pencil icon on your last message — verify content fills the input textarea and message is removed for re-editing
13. Edit the message and resubmit — verify new response streams
14. Open browser DevTools, set viewport to 375px width
15. Verify hamburger menu appears in mobile header
16. Click hamburger — verify sidebar opens as overlay (Sheet)
17. Verify chat input bar is visible and usable at 375px
18. Verify no horizontal scroll at any point
19. Run `pnpm test -- --run` in terminal — verify all tests pass

Resume signal: Type "approved" or describe any issues to fix.
  </action>
  <verify>Human confirms all 19 verification steps pass</verify>
  <done>Phase 2 chat experience verified by human: streaming works, campaigns persist, edit-last-message works, mobile layout is usable, all tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- `pnpm test -- --run` passes with all tests green (no todos remaining)
- `pnpm build` succeeds
- Error states display user-friendly messages with retry option
- Loading skeletons appear during streaming
- Mobile layout at 375px has no horizontal scroll
- Campaign validation happens before message persistence (no orphaned messages)
</verification>

<success_criteria>
Phase 2 is complete when: (1) all 13 requirement IDs are addressed, (2) all tests pass, (3) streaming chat works end-to-end with Claude, (4) rich content parts render correctly, (5) error handling shows friendly messages with retry, (6) mobile layout is usable at 375px, (7) edit-last-message works, (8) human verification confirms the experience.
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-core/02-05-SUMMARY.md`
</output>
