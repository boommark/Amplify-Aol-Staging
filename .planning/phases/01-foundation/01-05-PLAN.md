---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - supabase/migrations/001_foundation_schema.sql
  - supabase/seed/prompts.sql
autonomous: false
requirements: [AUTH-04, INFRA-06]

must_haves:
  truths:
    - "RLS policies correctly isolate data by role -- teacher sees only own campaigns"
    - "Staging deployment at staging.amplifyaol.com is accessible"
    - "Google OAuth works end-to-end on staging"
    - "Onboarding flow completes and profile is saved"
  artifacts:
    - path: "supabase/migrations/001_foundation_schema.sql"
      provides: "Applied migration -- verified by RLS query returning rowsecurity=true for all tables"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/seed/prompts.sql"
      provides: "Applied seed data -- verified by SELECT count(*) from prompts returning >= 5"
      contains: "INSERT INTO prompts"
  key_links:
    - from: "staging deployment"
      to: "supabase/migrations/001_foundation_schema.sql"
      via: "migration applied to Supabase project before deploy"
      pattern: "rowsecurity = true"
    - from: "staging deployment"
      to: "middleware.ts"
      via: "Vercel deploys middleware for auth protection"
      pattern: "staging.amplifyaol.com"
---

<objective>
Verify the complete Phase 1 foundation works end-to-end: apply the database migration to the Supabase project, seed prompts, deploy to staging, and verify Google OAuth, RLS, onboarding, and S3 presigned URLs all function correctly.

Purpose: This is the integration verification that proves Phase 1 delivers a secure, role-aware application shell. No Phase 2 work should begin until this passes.
Output: Working staging deployment with all Phase 1 requirements validated.
</objective>

<execution_context>
@/Users/abhishekratna/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abhishekratna/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- From Plans 01-04 -- all artifacts that must exist before verification -->

From Plan 01: lib/supabase/{client,server,middleware,admin}.ts, supabase/migrations/001_foundation_schema.sql, vercel.json
From Plan 02: middleware.ts, app/(auth)/login/page.tsx, app/(auth)/auth/callback/route.ts, app/(app)/onboarding/page.tsx, app/(app)/chat/page.tsx, app/(app)/admin/page.tsx
From Plan 03: lib/s3/presigned-url.ts, app/api/s3/presigned-url/route.ts
From Plan 04: supabase/seed/prompts.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply database migration, seed prompts, and deploy to staging</name>
  <files>supabase/migrations/001_foundation_schema.sql, supabase/seed/prompts.sql</files>
  <read_first>
    - supabase/migrations/001_foundation_schema.sql (migration to apply)
    - supabase/seed/prompts.sql (seed data to apply)
    - vercel.json (Fluid Compute config)
    - .env.local (env vars for Supabase project)
    - package.json (verify build script)
  </read_first>
  <action>
1. Verify the project builds successfully:
   ```bash
   pnpm build
   ```
   Fix any build errors before proceeding.

2. Apply the database migration to the Supabase project. The Supabase project ID is in NEXT_PUBLIC_SUPABASE_URL. Use one of:
   - If Supabase CLI is installed: `supabase db push` (reads from supabase/migrations/)
   - If not: Copy the SQL from `supabase/migrations/001_foundation_schema.sql` and execute it in the Supabase SQL Editor dashboard (https://supabase.com/dashboard/project/kmibwrgfskthkxhilutm/sql)
   - After applying, verify RLS is enabled: Run `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public';` -- every application table must show `rowsecurity = true`

3. Apply the prompt seed data:
   - Execute `supabase/seed/prompts.sql` against the Supabase project
   - Verify: `SELECT key, version, is_active FROM prompts ORDER BY key;` should return all seeded prompts

4. Seed the allowed_emails table with at least one email for testing:
   ```sql
   INSERT INTO allowed_emails (email) VALUES ('abhishek@example.com');
   ```
   (Replace with Abhishek's actual Google email)

5. Verify Google OAuth provider is configured in Supabase Auth settings:
   - Check Supabase Dashboard > Authentication > Providers > Google is enabled
   - Confirm callback URL is set to match the app's callback route

6. Deploy to Vercel (staging):
   ```bash
   vercel --prod
   ```
   Or push to the staging branch/repo if using git-based deploys.
   Verify staging.amplifyaol.com is accessible.

7. Run RLS verification queries to confirm data isolation works:
   ```sql
   -- Verify no table has RLS disabled:
   SELECT tablename FROM pg_tables
   WHERE schemaname = 'public' AND rowsecurity = false;
   -- Should return 0 rows
   ```

8. Verify staging deployment responds:
   ```bash
   curl -s -o /dev/null -w "%{http_code}" https://staging.amplifyaol.com/login
   ```
   Expected: 200
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build exits with code 0
    - All tables in Supabase have rowsecurity = true
    - Prompts table contains seeded records (at least 5 rows)
    - allowed_emails table contains at least one email
    - staging.amplifyaol.com loads (or Vercel deployment URL is accessible)
    - curl to staging login page returns HTTP 200
  </acceptance_criteria>
  <done>Database migration applied, prompts seeded, project builds, staging deployed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 foundation end-to-end</name>
  <files></files>
  <action>Human verification of complete Phase 1 foundation on staging deployment.</action>
  <what-built>Complete Phase 1 foundation: Google OAuth login, email allowlist gating, role assignment, onboarding, admin route guard, RLS, prompt registry, S3 presigned URLs, Vercel Fluid Compute, staging deployment.</what-built>
  <how-to-verify>
    1. Visit staging.amplifyaol.com (or Vercel preview URL)
    2. Verify login page shows with Amplify branding and "Sign in with Google" button
    3. Click "Sign in with Google" and complete the OAuth flow
    4. If your email is on the allowlist: verify you are redirected to onboarding
    5. Complete onboarding: enter display name, select a region, click Continue
    6. Verify you land on the chat placeholder page showing your display name
    7. Verify the sidebar is visible with "New Campaign" placeholder and your name
    8. Open a new tab to the same URL -- verify you are still authenticated (session persistence)
    9. Sign out and sign back in -- verify you go directly to chat (not onboarding again)
    10. Test with a non-allowlisted email (if possible): verify Request Access form appears
    11. Navigate to /admin -- verify you are redirected to /chat (unless you are admin role)
    12. In Supabase dashboard: verify the profiles table has your record with role='teacher', display_name, and region
    13. In Supabase dashboard: verify prompts table has seeded records
    14. Test the S3 presigned URL endpoint: `curl -X POST {staging_url}/api/s3/presigned-url -H "Content-Type: application/json" -d '{"key":"test.txt","contentType":"text/plain"}'` -- should return 401 (unauthenticated)
  </how-to-verify>
  <verify>Human verification of staging deployment</verify>
  <done>All 14 verification steps pass. Phase 1 foundation confirmed working.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- pnpm build succeeds
- All Supabase tables have RLS enabled
- Google OAuth flow completes end-to-end
- Session persists across refresh and new tabs
- Non-allowlisted users see Request Access form
- Non-admin users blocked from /admin routes (AUTH-05)
- Onboarding saves profile with display_name and region
- Sidebar placeholder visible with campaign list TODO
- Prompts table has seeded v1 records
- S3 presigned URL endpoint rejects unauthenticated requests
</verification>

<success_criteria>
- Phase 1 success criteria from ROADMAP are met:
  1. User can sign in with Google and stay signed in across refresh and tabs
  2. Each role sees only permitted data (RLS verified)
  3. Unauthenticated users redirected to login
  4. All AI prompts are stored and retrievable from Supabase with versioning
  5. Staging deployment at staging.amplifyaol.com is live with environment-specific config
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
