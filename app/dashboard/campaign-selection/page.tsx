import { CampaignSelection } from "@/components/campaign-selection/campaign-selection"

export default function CampaignSelectionPage() {
  // Remove any header elements from this page component
  return <CampaignSelection />
}
